# 实际浏览器验收记录

环境：Windows，本地 HTTP 服务，Codex 内置浏览器。操作日期 2026-09-07—08。没有通过浏览器发起真实支付、登录店铺或创建外部订单。

| 场景 | 实际操作与结果 |
| --- | --- |
| 首页 / 空状态 | 中文首页、示例输入、空方案正常；不能确认空清单 |
| 加载状态 | 请求进行时输入与发送按钮禁用，显示正在检索；请求完成恢复 |
| 明确单品 | 点击“300 元内宿舍键盘”，获得 1 项测试键盘，单价 159 元，未强制多件 |
| 组合澄清 | “1000 元改善桌面，已有笔记本和鼠标”得到一个重点澄清；回答照明和打字后生成键盘+台灯，已知小计 258 元，无重复鼠标 |
| 对话修改 | 输入“预算降到 300 元”，方案预算变为 300，条目保留、余额 42 元（未扣未知费用） |
| 手动修改 | 移除台灯，小计 159 元；替换为蓝牙键盘，小计 229 元，规格变为 Bluetooth / 78 键 |
| 持久化 | 页面刷新后，修订 8、预算 300、蓝牙键盘 229 元均恢复 |
| 超预算错误 | 尝试增加蓝牙键盘数量，显示超预算错误；原数量 1、小计 229 元保留 |
| 确认 | 面板列出 TEST-KBD1、规格、数量、CNY、159 元、运费未知和不履约说明；勾选后确认 |
| 购买入口 | 确认后创建本地演练购物车，跳到实际返回的本地 /checkout/… 页 |
| 演练完成 | 点击“完成演练（不付款）”；随后重新读取页面，显示“本地演练完成；没有付款或真实订单” |
| 故障 | /lab 注入检索超时，显示真实错误提示，进入 /runs；错误分类为检索，原蓝牙键盘方案保留 |
| 运行页 | 可见成功和失败运行、时间、模型模式、延迟，展开工具与证据 |
| 评测页 | 页面显示实际 47/47 系统测试；真实模型未运行，真实成交不可测；D07/H14 前后条目可追溯 |
| 桌面 | 1440×1000 双栏布局、历史侧栏和确认卡片正常 |
| 窄屏 | 390×844 上下排列，偏好/新任务、卡片、数量、替换、确认均可见；测得 documentWidth 375 ≤ viewport 390，无横向溢出 |
| 前端异常 | 最后读取浏览器 error 日志为空 |

修复：最初 Windows 默认编码导致策略读取失败，显式 UTF-8 后单品流程通过；补充澄清含预算的回归检查；窄屏补上新任务与偏好入口。对应代码测试与记录见 docs/evaluation.md。

这是本地端到端验收，不等价于外部模型或 Shopify 验收。后者缺配置，未运行。未做真实设备 Safari/Android 测试，也未开展真实用户试用。
