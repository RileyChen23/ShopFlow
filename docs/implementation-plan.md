# 实施记录

2026-09-07。工作区仅有 .git，无既有应用或 AGENTS.md。Python 3.13.5、Node 24 可用。

1. 核实模型工具调用、精选商品字段、Shopify 测试购物车依赖。
2. Python 标准库 HTTP 服务 + SQLite，单 Agent 工具循环；独立离线规则测试器。
3. 中文对话与方案工作台、修订冲突、偏好与历史、确认和购买状态。
4. 固定评测、风险逻辑测试、开发集发现问题后保留 v1/v2。
5. 浏览器实际操作、整理演示与未完成集成项。

取舍：无需安装第三方依赖；本地/持久卷部署，不使用临时无服务器文件系统。公开网页自动抓取不在首版运行时开放；由经过核实的结构化资料检索替代。数据维护通过 CLI 导入。

已检查配置存在性（未打印值）：LLM_API_KEY、LLM_BASE_URL、LLM_MODEL、SHOPIFY_STORE、SHOPIFY_STOREFRONT_TOKEN 均未设置。继续本地实现，不将离线规则或本地演练购物车称为真实模型或 Shopify 成功。
