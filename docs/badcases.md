# Bad case 复盘

本轮仅静态归因和确定性验证，新增模型请求为0。原始报告、Prompt和固定断言保持原样。

失败链路按：需求理解 → 规划/路由 → 工具选择与参数 → 商品检索与证据 → 预算/兼容计算 → 任务状态更新 → 回复生成 → 确认/购买边界 → 评测/运行基础设施。下面只标出有证据的失败层，未列出的层没有证据证明失败。

特别更正：此前把L03归因于用途追问过度，证据不足；日志直接理由是拒绝用测试夹具规划演练。两版都禁止将测试资料冒充真实，不能据一次退步锁定哪条新Prompt造成。

状态说明：已修复仅用于确定性基础设施修复或历史同次恢复；不代表模型行为已改善。所有本轮模型改动待真实复测。

## BC-L02 · 方案已暂存，最后额度又用于检查，整轮未提交

**状态**：待处理；**版本变化**：live-baseline：失败 → live-improved：通过；本轮badcase-contract-v1：待真实复测。

- [运行记录](../reports/live-eval-1788879902459415200.json) · run 6896bfabc0d0450983f451f07003efbe · 2026-09-08T15:05:22.205527+00:00 · live · Prompt live-baseline
- [运行记录](../reports/live-eval-1788880562625025900.json) · run ae8a625f5ef04af38ac8426cd885e4ef · 2026-09-08T15:16:06.330363+00:00 · live · Prompt live-improved

**用户输入与必要上下文**：预算1000元改善学习桌面，已有笔记本和鼠标；第二轮明确优先照明和打字、不买显示器。

**预期行为**：读取证据、更新两项方案后在5次请求内结束；不额外重复已有确定性检查。

**实际行为**：第二轮4次请求后set_plan返回两项、totals和compatibility，第5次又调用check_plan，没有最终回复，run抛出循环上限。

**用户影响**：任务失败、效率降低。已经选出的两项未提交，用户只能重试。

**失败链路**：规划/路由 → 工具结果复用 → 终止条件 → 状态提交

**直接证据**：

- 6896…：search_products×2 → read_evidence×4 → update_constraints → set_plan(2项) → check_plan。所有工具成功；失败点是没有剩余请求生成最终回复。
- set_plan 已返回已知总额、未知费用、over_budget及compatibility；check_plan重做这些计算，没有改变方案。
- 未观察到相同工具无限重复；只证明一次冗余检查挤占最后请求。此前第一轮还在广泛搜索后提出多个问题。

**根因**：末次请求仍开放工具，缺少确定性收尾约束；模型没有复用set_plan的检查结果。硬上限是触发失败的条件，不是数据或预算计算错误。

**置信度**：高：事件顺序与返回字段直接可见。不能凭单次轨迹证明所有冗余检查均来自同一模型机制。

**根因、诱因与现象**：根因候选：结果复用/终止契约；诱因：5次请求上限与此前的串行步骤；表象：最终结果未提交。

**修复方案**：最后一次允许请求不再提供工具；若模型仍发工具调用，拒绝执行并记录终止条件错误。set_plan返回显式validation与next_action。没有提高次数或编造成功回复。

**修改层级**：状态机/终止条件，随后工具输出契约

**为什么选这一层**：已有方案与计算都正确，先保护最后回复额度，比放宽上限或再次泛化Prompt更直接。

**复测结果**：本轮用脚本模型重放5步链路，检查最后tools为空、真实set_plan结果回传且不增加请求；真实复测未运行。历史live-improved的L02通过，共7次请求（两轮）。

**相关回归**：历史L03从通过变为失败，不能宣布前次提示整体提升。本轮拟与L03、保留L05一起复测。

**遗留风险**：最后额度到达时可能仍未构成方案；必须如实未完成，不保证所有需求5次足够。

## BC-L03 · 检索到合格测试候选，却拒绝将夹具加入演练方案

**状态**：回归退步；**版本变化**：live-baseline：通过 → live-improved：失败；live-bc-review：未运行。

- [运行记录](../reports/live-eval-1788879902459415200.json) · run 8ea8255b6e3847c3ad108ce719f63c75 · 2026-09-08T15:05:31.115033+00:00 · live · Prompt live-baseline
- [运行记录](../reports/live-eval-1788882341253307300.json) · run 263a43d56dd14ac1ba7078858a69ab52 · 2026-09-08T15:45:41.275080+00:00 · live · Prompt live-improved
- [运行记录](../reports/live-eval-1788882341253307300.json) · run 100f31cc68634506a750e3c520bfc4cc · 2026-09-08T15:45:50.004587+00:00 · live · Prompt live-improved

**用户输入与必要上下文**：预算1000元，选一把USB-A键盘和一盏台灯；台灯先不买，预算200元，保留键盘。scope=demo。

**预期行为**：在明确的演练范围内读取测试依据并生成标明虚构的方案；第二轮保留159元键盘。

**实际行为**：改进版检索后停在问是否演练；未调用read_evidence或set_plan。第二轮仅get_task/update_constraints，仍是空方案。

**用户影响**：任务失败；第二轮预算更新成功，但没有可保留的键盘。

**失败链路**：执行上下文 → 规划/路由 → 回复生成；状态机按未建方案工作

**直接证据**：

- 263a…第一轮4次请求，USB-A 接口精确搜索0条，放宽为键盘全类得到2条，台灯2条。
- 短输出：‘由于这些资料都明确注明是测试资料而非真实商品价格，我不能……排入采购方案。’随后问‘是否继续在demo环境测试流程’。
- 100f…第二轮2次请求：get_task(items=[])、update_constraints(budget_minor=20000)；数量与品类断言失败。
- baseline 8ea8…在同样夹具下读取依据并set_plan两项；因此不是目录没有合格商品。

**根因**：模型把‘不能把测试资料称为真实商品’扩大为‘不能规划演练’，而系统只给scope=demo，没有明确fixture_planning_allowed。对演练授权的理解不一致。

**置信度**：高：直接拒绝理由明确。中：显式演练语义不足是可修复诱因。待确认：是否哪条新增Prompt导致，因为禁止冒充真实的句子两版共有，单次随机输出不能识别因果。

**根因、诱因与现象**：不是已证实的用途不清。零命中增加了一次搜索但恢复后仍拒绝，属于诱因；过度追问是表象。

**修复方案**：模型任务视图明确purpose与fixture_planning_allowed；单独候选Prompt只澄清demo可以规划夹具、real仍隔离，明确已具备条件的组合不额外问偏好。保留L02的结果复用与末次收尾。

**修改层级**：服务端执行上下文；然后候选Prompt条件说明

**为什么选这一层**：先把真实/演练授权讲清楚，不用关键词硬塞方案，也不绕过证据校验。

**复测结果**：确定性测试验证demo允许夹具规划语义、real不允许，候选Prompt独立命名未改旧文件。未发送真实复测，不能标为修复。

**相关回归**：拟先跑目标L03（两轮），邻近L02（两轮），关键保留L05（一轮）；旧断言保留。

**遗留风险**：新增上下文可能仍被忽略；Prompt归因仍须对照，不把相关性说成因果。

## BC-UNIT · 50000分被回复为50000元

**状态**：仍失败；**版本变化**：协议原始成功/金额错误 → 新显示契约已测，模型未复测。

- [运行记录](../reports/protocol-1788878777288169500.json) · run protocol-1788878777288169500 · 2026-09-08T14:46:17.288186+00:00 · live · Prompt 未记录（协议探测）

**用户输入与必要上下文**：先调用get_task读取当前任务，再说明预算；服务器budget_minor=50000，CNY。

**预期行为**：CNY 500.00元；区分单位而非只证明工具回传。

**实际行为**：协议status=success，但回复‘50000……即50,000元人民币’。

**用户影响**：结果不可信；本次探测未创建采购方案或交易。

**失败链路**：工具返回单位契约 → 回复生成；确定性存储没有错

**直接证据**：

- 工具原始结果budget_minor=50000，totals.currency=CNY；没有错误乘法或预算状态变更。
- 报告只断言tools_selected与tool_result_returned，未检查金额语义。
- 历史业务L01检查159元通过，但这不是原协议探测的同条件修复证明。

**根因**：minor虽出现在字段名，却没有明确机器换算后的显示值；生成层误解释。协议成功指标没有覆盖业务语义。

**置信度**：高：原始值、回复和断言可比对。无法从日志证明模型内部如何理解minor。

**根因、诱因与现象**：根因层：输出契约/显示转换；诱因：无业务提示的探测；表象：百倍金额差。

**修复方案**：保留整数minor存储，schema注明分；用Decimal在服务端生成amounts，提供scale=100、major_units与display，null保留未知。初始任务和所有工具回传使用同一转换。

**修改层级**：工具schema、服务端确定性展示转换

**为什么选这一层**：单位不应由模型猜测；不把Prompt说明作为唯一防线。

**复测结果**：确定性测试覆盖50000→CNY500.00、零、null、币种和输入不变；原协议语义仍待3次真实探测。

**相关回归**：L01金额邻近案例以及USD参考报价不混入CNY需复核。

**遗留风险**：模型仍可能忽略display，所以实际回复正确性仍待检查；协议旧success不能等同金额通过。

## BC-SUBSET · 把无线USB筛选出的2项说成整个键盘目录

**状态**：仍失败；**版本变化**：live-improved浏览器：错误；badcase-contract-v1：模型未运行。

- [运行记录](../reports/browser-live-runs.json) · run d140fc3df5c3405c903d98428c784b12 · 2026-09-08T15:31:57.283117+00:00 · live · Prompt live-improved

**用户输入与必要上下文**：优先打字和照明，一个键盘和一个台灯，有USB接口，未知价格待核。真实资料/CNY。

**预期行为**：说明筛选范围；不能从无线USB子集推断整个目录只有2款。

**实际行为**：最终称‘数据库现有键盘里除了K270，只有……K400’。

**用户影响**：结果不可信，用户可能错过已有候选。

**失败链路**：商品检索结果契约 → 回复生成

**直接证据**：

- d140…先查外接键盘0条，再查‘键盘 无线 USB’2条；机械键盘0条。
- 历史返回是裸数组，没有total、matched、scope或filters。该数据快照实际有6个真实键盘型号；CNY范围5个报价，GBP1个不在本任务内。
- 过滤正确返回2条不等于目录只有2款；不是检索服务超时。

**根因**：模型把匹配子集外推为全集；裸数组没有显式范围和分母，加剧错误。

**置信度**：高：查询、返回数、目录快照和原话均可追溯；返回元数据能否消除生成错误待真实验证。

**根因、诱因与现象**：检索过滤不是错；输出范围信息不足是契约缺陷，模型外推是直接生成错误。

**修复方案**：模型搜索回传items、scope、currency、scope_total、total、category_total、matched、returned、filters、truncated与count_unit=offers。零命中注明精确文本过滤，不静默放宽硬条件。

**修改层级**：工具输出契约；再补解释规则

**为什么选这一层**：先让模型知道分母与过滤条件，而不是只增加‘不要幻觉’。

**复测结果**：当前固定目录回放检验23个CNY报价/5个键盘报价/该过滤2个命中；空结果与故障分开。模型表述未复测。

**相关回归**：建议追加一轮真实限定查询（上限5次），单独检查范围陈述与商品依据。

**遗留风险**：报价数不是型号数；多报价与币种过滤仍须按count_unit理解。

## BC-EVIDENCE · 未读取证据先set_plan，被后端拦截后恢复

**状态**：已修复；**版本变化**：live-improved：越序被拦截→同次恢复（防线已验证）。

- [运行记录](../reports/browser-live-runs.json) · run d140fc3df5c3405c903d98428c784b12 · 2026-09-08T15:31:57.283117+00:00 · live · Prompt live-improved

**用户输入与必要上下文**：与BC-SUBSET同一浏览器任务。

**预期行为**：先读取所选报价依据，再写方案。

**实际行为**：第一次set_plan被拒；模型随后读取两项证据，再次set_plan成功；运行status=partial保留错误。

**用户影响**：额外调用和延迟；未让无依据条目提交。

**失败链路**：工具选择顺序 → 状态更新防线

**直接证据**：

- d140…set_plan错误：‘推荐前必须读取报价依据’；随后read_evidence(real-k270-o)、read_evidence(real-lamp1s-o)，第二次set_plan成功。
- 失败未改变暂存方案；同一运行6次请求，无静默伪成功。

**根因**：模型越过必需证据读取步骤；现有后端前置条件有效。

**置信度**：高：拒绝与恢复均有工具事件。

**根因、诱因与现象**：工具路由错误不是商品缺失；已修复仅指本次错误被防线拦截并恢复，不指模型不再越序。

**修复方案**：保留前置校验，不把搜索结果等同读证据成功；搜索契约显式evidence_required_before_plan。

**修改层级**：既有工具权限/前置条件防线；补工具输出说明

**为什么选这一层**：不能为节约调用而放开无证据写入。

**复测结果**：历史同次运行已恢复；本轮确定性回归检查拒绝写入且不改变状态。

**相关回归**：邻近L02继续验证证据读取条件，不能因终止改动放行越序。

**遗留风险**：模型越序行为仍可能复发；防线已验证≠模型路由已优化。

## BC-REMOVE · 预算修复已移除台灯，模型又删除一次

**状态**：待处理；**版本变化**：baseline最终通过但有错误事件；新契约待真实复测。

- [运行记录](../reports/live-eval-1788879902459415200.json) · run 60d4a28887574000a767dd20b7f7f8dd · 2026-09-08T15:05:40.072977+00:00 · live · Prompt live-baseline

**用户输入与必要上下文**：预算从1000元降为200元，移除台灯保留键盘。

**预期行为**：读取预算修复后的当前条目，避免删除已不存在的台灯。

**实际行为**：update_constraints修复预算后已移除台灯，紧接update_item删除失败，再get_task恢复。

**用户影响**：多一次错误恢复；最终方案正确，但浪费请求。

**失败链路**：任务状态更新副作用 → 工具结果理解

**直接证据**：

- 60d4…同一工具批次：update_constraints(20000)，update_item(test-lamp1-o,remove=true)报‘条目不在当前方案’。
- 之后get_task显示仅test-kbd1-o；最终159元键盘，原案例通过。

**根因**：update_constraints有过滤和预算修复副作用，旧结果给removed名称与totals，却没有当前offer_id列表；模型也在同一批预先决定了依赖前一工具结果的删除。

**置信度**：高：代码副作用与顺序匹配。中：单纯补返回字段不一定避免同批依赖调用。

**根因、诱因与现象**：计算没有错；是状态副作用和依赖工具规划不匹配。

**修复方案**：回传current_items的报价ID/数量/必要性，并注明修复已执行；候选提示要求依赖修改读取前一步结果。保持不存在条目报错，不把错误改成成功。

**修改层级**：工具状态返回与依赖调用说明

**为什么选这一层**：静默幂等删除会掩盖模型对状态的误判。

**复测结果**：确定性测试验证降预算后返回的ID列表不含台灯，后续错误不破坏键盘；真实复测由L03覆盖。

**相关回归**：目标L03第二轮关注错误事件是否消失，既有最终条目断言保留。

**遗留风险**：同一批依赖工具仍可能冲突；不声称补字段后已消失。

## BC-L05-BUDGET · 批次剩余2次被当成该案例的每轮上限

**状态**：已修复；**版本变化**：首批资源不足失败→补批通过（条件不同）；调度缺陷代码已修复。

- [运行记录](../reports/live-eval-1788879902459415200.json) · run f2fa925daf774a57828515c7a39eb256 · 2026-09-08T15:05:51.431968+00:00 · live · Prompt live-baseline
- [运行记录](../reports/live-eval-1788880951052046500.json) · run bf66303d14394671994919178a4dfc23 · 2026-09-08T15:22:31.069965+00:00 · live · Prompt live-baseline

**用户输入与必要上下文**：只有USB-C、不知道笔记本型号，预算500元，扩展坞接屏能保证吗？

**预期行为**：相同每轮5次条件；不足时整例不开始并单独标资源不足。

**实际行为**：首批L05只得到2次请求，在读依据/更新约束后耗尽。补批5次条件下通过。

**用户影响**：误读模型能力；资源不足与普通任务失败混在一起。

**失败链路**：评测/运行基础设施

**直接证据**：

- f2fa…model_calls=2；evaluate_live原代码LLM_MAX_CALLS=min(5,remaining())。
- bf66…补批5次，最终明确‘不能保证’，询问主机型号和显示器接口。

**根因**：评测器把剩余批次额度悄悄压缩为单案例条件，导致不可比。

**置信度**：高：脚本分支和两份轨迹一致。不证明给更多额度必然通过所有任务。

**根因、诱因与现象**：根因是调度条件变化，不是已证实的模型兼容判断错误。

**修复方案**：开始案例前预留每轮5次×轮数；不足整例not_run/resource_limited，不发请求、不缩小条件。新报告记录evaluation_policy_version。

**修改层级**：评测调度，不改模型上限

**为什么选这一层**：保留可比较实验条件，而不是删除失败报告或扩大所有模型请求上限。

**复测结果**：确定性测试确认不足额度时agent.run不会被调用；历史首次失败不改，补跑单列不可当一次独立首测。

**相关回归**：保留L05在新协议下复测，检查兼容性与追问边界。

**遗留风险**：保守预留可能留下未用额度；新调度策略须标明，历史2次与5次不是同条件。

## BC-STATE · 方案排除了已有物品，但任务约束没有保存

**状态**：仍失败；**版本变化**：live-improved旧成绩通过，补充状态审计不通过；修复未验证。

- [运行记录](../reports/live-eval-1788880562625025900.json) · run d854b235164a441d8bacd78c72205ee6 · 2026-09-08T15:16:02.645165+00:00 · live · Prompt live-improved
- [运行记录](../reports/live-eval-1788880562625025900.json) · run ae8a625f5ef04af38ac8426cd885e4ef · 2026-09-08T15:16:06.330363+00:00 · live · Prompt live-improved

**用户输入与必要上下文**：已有笔记本和鼠标，后续明确不用显示器。

**预期行为**：owned与excluded保存在同一份任务状态，后续修改仍生效。

**实际行为**：L02全部旧断言通过，但最终owned=[]、excluded=[]、constraints=[]。

**用户影响**：结果状态不完整，后续可能重新推荐已排除商品。没有实际观察到再次推荐，风险不说成已发生。

**失败链路**：需求理解/工具参数 → 任务状态更新 → 评测漏检

**直接证据**：

- d854…update_constraints只写budget_minor=100000、recipient=self；第二轮没有补约束。
- ae8a…最终items为键盘/台灯，owned/excluded/constraints均空。旧exclusions断言只验证当前items的品类。

**根因**：模型遗漏约束更新参数；评测只检查当前方案，没有检查多轮状态持续性。

**置信度**：高：输入、参数、状态和断言代码均明确。

**根因、诱因与现象**：当前推荐没有重复≠长期约束已记住；不是数据库把已写字段丢失。

**修复方案**：本轮只补独立状态审计/检查清单，不用关键词规则替模型回填。候选Prompt提醒每次先同步明确约束；真实修复待验证。新检查不倒改旧passed。

**修改层级**：评测覆盖先补；模型状态参数遗漏仍待处理

**为什么选这一层**：缺字段并不能靠金额计算或数据库修复解决，需检查模型是否实际调用了正确参数。

**复测结果**：确定性审计复现旧通过记录的约束缺失；没有把此记录改成新的历史失败。

**相关回归**：L02复测除旧断言外，单列owned包含笔记本/鼠标、excluded含显示器；新增口径与旧成绩不可整体直接比较。

**遗留风险**：Prompt提醒未必足够；真实复测后再决定是否引入结构化约束确认，不在本轮扩功能。

## 请求预算与复测计划（等待确认）

核心最小集合为L03两轮（预留10次）、L02两轮（预留10次）、保留L05一轮（预留5次），每轮上限仍为5次，总预留25次；实际调用可能更少，不能保证模型最低需要几次。用途依次是演练授权修复、终止/约束保存回归、兼容性与真实/测试边界。

原剩余5次未经本轮确认不使用。若确认25次总预留，需另授权20次。按原保守峰值费率上界约4.95元；可选金额协议3次（连接/工具选择/回传金额）与子集单轮5次，合计33次上界6.534元。费率是2026-09-08配置，不是实际扣费；执行前复核有效费率。

候选Prompt为live-bc-review，旧live-baseline/live-improved文件不改。新增工具契约与调度版本单独记录。此轮是组合修复的回归，不是能识别各层独立贡献的消融实验；如需因果确认，应另设计对照并另批额度。报告按案例分别列修复、未解决、退步、资源不足/不可比较，不能拼接最好批次宣称整体提升。

本轮确定性结果见reports/badcase-validation.json；界面数据源为reports/badcases.json。面试材料独立交付E:\实习\badcase-interview.md，不放在项目docs目录。
